"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.InterruptError = exports.ModuleTimeoutError = exports.LogLevel = void 0;
exports.setupSynchronousClient = setupSynchronousClient;
const worker_threads_1 = require("worker_threads");
const path = __importStar(require("path"));
const events_1 = require("events");
const util_1 = require("util");
const child_process_1 = require("child_process");
const fs = __importStar(require("fs/promises"));
const util = __importStar(require("util"));
var LogLevel;
(function (LogLevel) {
    LogLevel[LogLevel["None"] = -1] = "None";
    LogLevel[LogLevel["Error"] = 0] = "Error";
    LogLevel[LogLevel["Warning"] = 1] = "Warning";
    LogLevel[LogLevel["Info"] = 2] = "Info";
    LogLevel[LogLevel["Debug"] = 4] = "Debug";
})(LogLevel || (exports.LogLevel = LogLevel = {}));
const logLevelNames = {
    [LogLevel.None]: "None",
    [LogLevel.Error]: "Error",
    [LogLevel.Warning]: "Warning",
    [LogLevel.Info]: "Info",
    [LogLevel.Debug]: "Debug",
};
var MessageKind;
(function (MessageKind) {
    MessageKind[MessageKind["Control"] = 0] = "Control";
    MessageKind[MessageKind["Data"] = 1] = "Data";
})(MessageKind || (MessageKind = {}));
var ControllerControlMessageKind;
(function (ControllerControlMessageKind) {
    ControllerControlMessageKind[ControllerControlMessageKind["Shutdown"] = 0] = "Shutdown";
})(ControllerControlMessageKind || (ControllerControlMessageKind = {}));
var ExecutorControlMessageKind;
(function (ExecutorControlMessageKind) {
    ExecutorControlMessageKind[ExecutorControlMessageKind["Init"] = 0] = "Init";
    ExecutorControlMessageKind[ExecutorControlMessageKind["Shutdown"] = 1] = "Shutdown";
})(ExecutorControlMessageKind || (ExecutorControlMessageKind = {}));
function isModuleResponse(response) {
    return !!response && "moduleKey" in response;
}
var RequestKind;
(function (RequestKind) {
    RequestKind[RequestKind["LaunchCloudAgent"] = 0] = "LaunchCloudAgent";
    RequestKind[RequestKind["Connect"] = 1] = "Connect";
    RequestKind[RequestKind["Command"] = 2] = "Command";
    RequestKind[RequestKind["Cancel"] = 3] = "Cancel";
    RequestKind[RequestKind["Close"] = 4] = "Close";
    RequestKind[RequestKind["ConfigureLogging"] = 5] = "ConfigureLogging";
    RequestKind[RequestKind["Log"] = 6] = "Log";
})(RequestKind || (RequestKind = {}));
var ResponseKind;
(function (ResponseKind) {
    ResponseKind[ResponseKind["CloudAgentPort"] = 0] = "CloudAgentPort";
    ResponseKind[ResponseKind["ConsoleMessage"] = 1] = "ConsoleMessage";
    ResponseKind[ResponseKind["Connected"] = 2] = "Connected";
    ResponseKind[ResponseKind["ModuleMessage"] = 3] = "ModuleMessage";
    ResponseKind[ResponseKind["Closed"] = 4] = "Closed";
    ResponseKind[ResponseKind["LoggingConfiguration"] = 5] = "LoggingConfiguration";
})(ResponseKind || (ResponseKind = {}));
// Default error if none is provided
class ModuleTimeoutError extends Error {
    /** @internal */
    constructor() {
        super("Operation timed out");
    }
}
exports.ModuleTimeoutError = ModuleTimeoutError;
/**
 * @internal
 * An error that is thrown when an operation is interrupted
 */
class InterruptError extends Error {
    /** @internal */
    constructor(msg) {
        super(msg);
    }
}
exports.InterruptError = InterruptError;
function assertUnreachable(x) {
    // eslint-disable-next-line @typescript-eslint/restrict-template-expressions
    throw new Error(`unreachable reached: ${x}`);
}
function partial(fn, f) {
    return (...args) => fn.apply(null, [f, ...args]);
}
// override JSON serialization in this file (I want to avoid anything that could affect
// behavior in a user's script)
const JSON = {
    stringify: (data) => {
        function replacer(_, v) {
            return typeof v === "bigint" ? v.toString() : v;
        }
        return global.JSON.stringify(data, replacer);
    },
    parse: (str) => {
        function reviver(_, v) {
            // revive any bigints
            if (typeof v === "string" && v.startsWith("!bi:")) {
                return BigInt(v.substring(4));
            }
            return v;
        }
        return global.JSON.parse(str, reviver);
    }
};
// Used to identify to the worker thread that it was spawned by SyncAgent.
const SYNC_AGENT_GUID = "5F4CFABB-1EAB-4A10-9FEA-C8C75CF7442D";
function isExecutorInitData(data) {
    return typeof data === "object"
        && data !== null
        && ("SYNC_AGENT_GUID" in data)
        && data.SYNC_AGENT_GUID === SYNC_AGENT_GUID;
}
function setupSynchronousMessaging(requestSender, responseReceiver, responsesPending, TimeoutError) {
    function send(data) {
        requestSender.postMessage(data);
    }
    function receiveMessage() {
        const response = (0, worker_threads_1.receiveMessageOnPort)(responseReceiver)?.message;
        if (response) {
            Atomics.sub(responsesPending, 0, 1);
            processMessage(response);
        }
        return response;
    }
    const awaitedResponses = new Set;
    function awaitMessage(matcher) {
        let result;
        const awaiter = {
            process: (msg) => {
                if (result === undefined && matcher(msg)) {
                    result = msg;
                    return true;
                }
                return false;
            },
            interrupt: (msg) => { result = new InterruptError(msg); },
            cancel: () => awaitedResponses.delete(awaiter),
            pending: () => (result === undefined),
            response: () => {
                if (result === undefined) {
                    throw new Error("Internal Error: Still awaiting response");
                }
                return result;
            },
        };
        awaitedResponses.add(awaiter);
        return awaiter;
    }
    let onMessageCallback = () => { };
    function processMessage(msg) {
        awaitedResponses.forEach((awaiter) => {
            awaiter.process(msg);
            if (!awaiter.pending()) {
                awaitedResponses.delete(awaiter);
            }
        });
        onMessageCallback(msg);
    }
    function waitForMessage(timeoutAt) {
        let timeout = undefined;
        if (timeoutAt) {
            timeout = timeoutAt - Date.now();
            if (timeout <= 0) {
                throw new TimeoutError();
            }
        }
        const status = Atomics.wait(responsesPending, 0, 0, timeout);
        if (status === "timed-out") {
            throw new TimeoutError();
        }
        return receiveMessage();
    }
    function waitFor(matcher, timeoutAt) {
        const awaiter = awaitMessage(matcher);
        try {
            while (awaiter.pending()) {
                waitForMessage(timeoutAt);
            }
        }
        catch (err) {
            awaiter.cancel();
            throw err;
        }
        return awaiter.response();
    }
    function interruptWaits(msg) {
        for (const awaiter of awaitedResponses) {
            awaiter.interrupt(msg);
        }
    }
    async function waitForAsync() {
        const ret = Atomics.waitAsync(responsesPending, 0, 0);
        if (ret.async) {
            await ret.value;
        }
        return receiveMessage();
    }
    return {
        send,
        waitFor,
        waitForAsync,
        interruptWaits,
        setOnMessageCallback: (cb) => { onMessageCallback = cb; },
    };
}
// Sets up the executor thread and all the messaging between it and us
function setupExecutorThread(cloudAgentDir, inProcess, TimeoutError, onResponse, timeoutAt) {
    let ExecutorState;
    (function (ExecutorState) {
        ExecutorState["Initializing"] = "Initializing";
        ExecutorState["Running"] = "Running";
        ExecutorState["ShuttingDown"] = "Shutting Down";
        ExecutorState["Shutdown"] = "Shutdown";
    })(ExecutorState || (ExecutorState = {}));
    // Messaging scheme:
    // We create two MessageChannels, one for the main thread to send requests to the
    // worker, and the other for the worker to send back responses and events.
    // The worker thread will asynchronously handle requests sent by the main thread.
    // For communication back, we create a small shared buffer to keep track of the
    // messages pending on the response channel. To wait for a response, the main
    // thread will block until this number is non-zero. Atomic operations are used
    // to avoid race conditions on the shared buffer.
    //
    // Notes:
    // * We cannot use the worker's send method and the parentPort. It seems that they
    //   don't work when the main thread is blocked.
    // * We should not register a listener on the responseReceiver MessagePort. Doing
    //   so would cause the MessagePort's queue to empty if control is ever yielded to
    //   the Node.js event loop. (e.g. A script could await at the top level.)
    const { port1: requestSender, port2: requestReceiver } = new worker_threads_1.MessageChannel();
    const { port1: responseSender, port2: responseReceiver } = new worker_threads_1.MessageChannel();
    const sharedBuffer = new SharedArrayBuffer(4);
    const responsesPending = new Int32Array(sharedBuffer, 0, 1);
    const messaging = setupSynchronousMessaging(requestSender, responseReceiver, responsesPending, TimeoutError);
    function controlMessage(controlData) {
        return { kind: MessageKind.Control, controlData };
    }
    function dataMessage(data) {
        return { kind: MessageKind.Data, data };
    }
    function isControlMessage(msg) {
        return msg.kind === MessageKind.Control;
    }
    function isDataMessage(msg) {
        return msg.kind === MessageKind.Data;
    }
    const workerData = {
        sharedBuffer,
        requestReceiver,
        responseSender,
        cloudAgentDir: path.resolve(cloudAgentDir),
        inProcess,
        SYNC_AGENT_GUID,
    };
    const transferList = [requestReceiver, responseSender];
    // In-process communication requires setting the PATH so that dll's get loaded
    // properly. So we need the worker's changes to the environment variables to be
    // visible on the main thread and native add-ons.
    const env = inProcess ? worker_threads_1.SHARE_ENV : undefined;
    const worker = new worker_threads_1.Worker(__filename, { workerData, transferList, env });
    let state = ExecutorState.Initializing;
    // First message must be an indication that initialization was successful
    try {
        const anyResponse = () => true;
        const msg = messaging.waitFor(anyResponse, timeoutAt);
        if (!msg) {
            throw new Error("Internal Error: Received empty message during initialization");
        }
        if (msg instanceof InterruptError) {
            Error.captureStackTrace(msg); // capture stack trace at this point
            throw msg;
        }
        if (isControlMessage(msg) && msg.controlData.kind === ExecutorControlMessageKind.Init) {
            const { controlData: { error } } = msg;
            if (error) {
                throw new Error(`Internal Error: An error occurred during initialization: ${error}`);
            }
            state = ExecutorState.Running;
        }
        else {
            throw new Error("Internal Error: Received unexpected message during initialization");
        }
    }
    catch (err) {
        void worker.terminate();
        throw err;
    }
    messaging.setOnMessageCallback((msg) => {
        if (isDataMessage(msg)) {
            onResponse(msg.data);
        }
    });
    // By default we only process events while we are blocked waiting for a response. That
    // works well for a script where we might expect to be blocking often, but it is a
    // problem if we are doing it rarely and expecting to react to events. For example,
    // in the scripting console.
    // To address this, we constantly await on any pending message from the worker to
    // process the messages when they come in. Note that this will only happen when we
    // are not blocking, meaning that it will not interfere with our synchronous
    // behavior. This is purely to process events when control is being returned to the
    // event loop.
    async function asynchronouslyProcessPendingMessages() {
        while (state === ExecutorState.Running) {
            await messaging.waitForAsync();
        }
    }
    void asynchronouslyProcessPendingMessages();
    function send(data) {
        if (state !== ExecutorState.Running) {
            throw new Error(`Internal error: Executor is ${state}`);
        }
        messaging.send(dataMessage(data));
    }
    function receive(matcher, timeoutAt) {
        if (state !== ExecutorState.Running) {
            throw new Error(`Internal error: Executor is ${state}`);
        }
        const response = messaging.waitFor((msg) => isDataMessage(msg) && matcher(msg.data), timeoutAt);
        if (response instanceof InterruptError) {
            // The error was created at the point where we requested everything get interrupted
            // but it will be clearer to the user if the stack is captured closer to the
            // place where they called the function that blocked.
            // This isn't the best place place, but we can start here and improve it elsewhere.
            Error.captureStackTrace(response);
            throw response;
        }
        return response.data;
    }
    function interruptWaits(msg) {
        if (state !== ExecutorState.Running) {
            throw new Error(`Internal error: Executor is ${state}`);
        }
        messaging.interruptWaits(msg);
    }
    function shutdown() {
        if (state !== ExecutorState.Running) {
            return;
        }
        state = ExecutorState.ShuttingDown;
        messaging.send(controlMessage({ kind: ControllerControlMessageKind.Shutdown }));
        messaging.waitFor((msg) => isControlMessage(msg) && msg.controlData.kind === ExecutorControlMessageKind.Shutdown);
        state = ExecutorState.Shutdown;
        void worker.terminate();
    }
    return {
        send,
        receive,
        interruptWaits,
        shutdown,
    };
}
function setupSynchronousClient(cloudAgentDir, options) {
    const inProcess = options?.inProcess || false;
    const TimeoutError = options?.TimeoutError || ModuleTimeoutError;
    // We log to file on the async thread since the main thread can't do anything while blocked
    const logging = { threshold: LogLevel.None };
    function logMessage(logLevel, message) {
        if (logLevel <= logging.threshold) {
            executor.send({ kind: RequestKind.Log, message: `[${logLevelNames[logLevel]} - Client] ${message}` });
        }
    }
    const logger = {
        error: partial(logMessage, LogLevel.Error),
        warn: partial(logMessage, LogLevel.Warning),
        info: partial(logMessage, LogLevel.Info),
        debug: partial(logMessage, LogLevel.Debug),
    };
    function configureLogging(logFile, logThreshold = LogLevel.Info) {
        executor.send({ kind: RequestKind.ConfigureLogging, logFile, logThreshold });
        const logConfigResponse = ({ kind }) => kind === ResponseKind.LoggingConfiguration;
        const { error } = executor.receive(logConfigResponse, getTimeoutTime());
        if (error) {
            throw new Error(`Failed to configure logging: ${error}`);
        }
        logging.threshold = logThreshold;
    }
    let nextModuleId = 1;
    const modules = {};
    function handleModuleResponse(response) {
        const { kind, moduleKey } = response;
        switch (kind) {
            case ResponseKind.Connected:
                break;
            case ResponseKind.ModuleMessage:
                if ("event" in response.data) {
                    const { event, data } = response.data;
                    modules[moduleKey]?.emitEvent(event, data);
                }
                break;
            case ResponseKind.Closed:
                modules[moduleKey]?.onClose();
                break;
            default:
                assertUnreachable(kind);
        }
    }
    let timeout = 0;
    function getTimeoutTime() {
        return timeout > 0 ? Date.now() + timeout : undefined;
    }
    function getTimeout() {
        return timeout;
    }
    function setTimeout(ms) {
        if (typeof ms !== "number") {
            throw new TypeError("timeout value provided is not a number");
        }
        timeout = ms > 0 ? ms : 0;
        logger.info(`Timeout set to ${timeout}`);
    }
    function receiveForModule(moduleKey, matcher, timeoutAt) {
        return executor.receive((res) => isModuleResponse(res) && res.moduleKey === moduleKey && matcher(res), timeoutAt);
    }
    function createModule(port, subProtocol) {
        const subModules = {};
        const listeners = {};
        const moduleKey = nextModuleId++;
        function receiveResponse(waiter, timeoutAt) {
            return receiveForModule(moduleKey, waiter, timeoutAt);
        }
        function emitEvent(event, data) {
            const eventListeners = listeners[event];
            if (eventListeners) {
                for (const listener of eventListeners) {
                    if (listener) {
                        listener(data);
                    }
                }
            }
        }
        let execCommand;
        let onClose = () => {
            // Ensure we only call this once;
            onClose = () => { };
            execCommand = () => { throw new Error("Module Closed"); };
            logger.debug(`module has closed (port: ${port}, subProtocol: ${subProtocol || ''})`);
            delete modules[moduleKey];
            emitEvent("close");
        };
        function close() {
            executor.send({ kind: RequestKind.Close, moduleKey });
            receiveResponse((res) => res.kind === ResponseKind.Closed, getTimeoutTime());
        }
        let nextCommandId = 1;
        execCommand = (command, data) => {
            const commandId = nextCommandId++;
            const isCommandResponse = (res) => {
                const { kind } = res;
                if (kind === ResponseKind.Closed) {
                    return true;
                }
                else if (kind === ResponseKind.ModuleMessage) {
                    const { data } = res;
                    return ("response" in data && data.response === commandId)
                        || ("error" in data && data.error === commandId);
                }
                return false;
            };
            logger.info(`Calling ${command}(${data.map((dat) => util.inspect(dat)).join(", ")})`);
            executor.send({ kind: RequestKind.Command, moduleKey, data: { id: commandId, command, data } });
            let res;
            try {
                res = receiveResponse(isCommandResponse, getTimeoutTime());
            }
            catch (err) {
                if (err instanceof InterruptError) {
                    // We were interrupted, attempt to cancel the command
                    executor.send({ kind: RequestKind.Cancel, moduleKey, data: { cancel: commandId } });
                    // Make the error's stack simpler by removing our machinery
                    Error.captureStackTrace(err, execCommand);
                }
                throw err;
            }
            const { kind: resKind } = res;
            switch (resKind) {
                case ResponseKind.Closed:
                    throw new Error("Module Closed");
                case ResponseKind.ModuleMessage:
                    if ("response" in res.data) {
                        const { response: id, data: result } = res.data;
                        if (id === commandId) {
                            if (command !== "listCommands") {
                                logger.info(`${command}(${data.map((dat) => util.inspect(dat)).join(", ")}) returned ${util.inspect(result)}`);
                            }
                            return result;
                        }
                    }
                    else if ("error" in res.data) {
                        const { error: id, data: err } = res.data;
                        if (id === commandId) {
                            const error = new Error(err.message);
                            if (err.stack) {
                                error.stack = err.stack + "\n" + error.stack;
                            }
                            logger.info(`${command}(${data.map((dat) => util.inspect(dat)).join(", ")}) raised an error ${err.message}`);
                            throw error;
                        }
                    }
                    break;
                case ResponseKind.Connected:
                    throw new Error("Internal Error: Received unexpected response");
                default:
                    assertUnreachable(resKind);
            }
        };
        function addListener(event, listener) {
            if (!listeners[event]) {
                listeners[event] = [];
            }
            listeners[event].push(listener);
        }
        function removeListener(event, listener) {
            const eventListeners = listeners[event];
            if (eventListeners) {
                const i = eventListeners.indexOf(listener);
                if (-1 !== i) {
                    eventListeners[i] = null;
                }
            }
        }
        function createModulePart(prefix = "") {
            return {
                addListener: (event, listener) => addListener(prefix + event, listener),
                removeListener: (event, listener) => removeListener(prefix + event, listener),
            };
        }
        function addCommand(part, command, prefix = "") {
            const [first, ...rest] = command.split(".");
            if (rest.length === 0) {
                const commandFn = (...args) => execCommand(prefix + command, args);
                // Set the function name so it is displayed if inspected by the console
                Object.defineProperty(commandFn, "name", { value: command });
                part[first] = commandFn;
            }
            else {
                prefix += first + ".";
                if (!part[first]) {
                    part[first] = createModulePart(prefix);
                }
                addCommand(part[first], rest.join("."), prefix);
            }
        }
        function listCommands() {
            return execCommand("listCommands", []).commands;
        }
        function createSubModule(name) {
            const { port, subProtocol } = execCommand("createSubModule", [name]);
            const subModule = createModule(port, subProtocol);
            subModule.addListener("close", () => { delete subModules[name]; });
            return subModule;
        }
        function getSubModule(subModuleName) {
            if (subModuleName in subModules) {
                return subModules[subModuleName];
            }
            const subModule = createSubModule(subModuleName);
            subModules[subModuleName] = subModule;
            return subModule;
        }
        executor.send({ kind: RequestKind.Connect, port, subProtocol, moduleKey });
        receiveResponse(({ kind }) => kind === ResponseKind.Connected, getTimeoutTime());
        const module = { ...createModulePart(), getSubModule, close };
        for (const command of listCommands()) {
            addCommand(module, command);
        }
        modules[moduleKey] = { close, emitEvent, onClose };
        return module;
    }
    function waitForEvent(obj, event) {
        logger.info(`Waiting for event "${event}"`);
        let eventData = { received: false };
        const listener = (data) => { eventData = { received: true, data }; };
        obj.addListener(event, listener);
        try {
            const timeoutAt = getTimeoutTime();
            while (!eventData.received) {
                executor.receive(() => true, timeoutAt);
            }
            logger.info(`Received event "${event}"`);
            // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
            return eventData.data;
        }
        catch (err) {
            if (err instanceof TimeoutError) {
                logger.info(`Timed out waiting for event "${event}"`);
            }
            else {
                logger.info(`Unexpected error while waiting for event "${event}"`);
            }
            throw err;
        }
        finally {
            obj.removeListener(event, listener);
        }
    }
    function shutdown() {
        const toClose = Object.values(modules);
        for (const module of toClose) {
            module.close();
        }
        executor.shutdown();
    }
    function launchCloudAgent() {
        executor.send({ kind: RequestKind.LaunchCloudAgent });
        const isCAResponse = ({ kind }) => kind === ResponseKind.CloudAgentPort;
        const { data } = executor.receive(isCAResponse, getTimeoutTime());
        if ("port" in data) {
            return data.port;
        }
        else {
            throw new Error("Failed to start CloudAgent: " + data.error);
        }
    }
    function sleep(ms) {
        const timeoutAt = Date.now() + ms;
        try {
            executor.receive(() => false, timeoutAt);
        }
        catch (err) {
            if (!(err instanceof TimeoutError)) {
                throw err;
            }
        }
    }
    function onExecutorResponse(response) {
        if (isModuleResponse(response)) {
            handleModuleResponse(response);
        }
    }
    // Interrupt any awaited response with the provided error
    function interruptWaits(msg) {
        logger.debug(`Interrupting waits with message '${msg}'`);
        executor.interruptWaits(msg);
    }
    const executor = setupExecutorThread(cloudAgentDir, inProcess, TimeoutError, onExecutorResponse);
    // The worker thread will prevent node from closing
    process.on("exit", shutdown);
    // Configure logging if needed
    if (options?.logDestination) {
        configureLogging(options.logDestination, options?.logThreshold || LogLevel.Info);
    }
    else if (process.env.TI_DS_ENABLE_LOGGING && process.env.TI_DS_LOGGING_OUTPUT) {
        const dsLogFile = process.env.TI_DS_LOGGING_OUTPUT;
        const dir = path.dirname(dsLogFile);
        const ext = path.extname(dsLogFile);
        const basename = path.basename(dsLogFile, ext);
        configureLogging(path.join(dir, basename + "_sync" + ext), LogLevel.Debug);
    }
    return {
        launchCloudAgent,
        createModule,
        waitForEvent,
        getTimeout,
        setTimeout,
        shutdown,
        sleep,
        interruptWaits
    };
}
function runExecutorThread({ sharedBuffer, requestReceiver, responseSender, cloudAgentDir, inProcess }) {
    const responsesPending = new Int32Array(sharedBuffer, 0, 1);
    function send(msg) {
        responseSender.postMessage(msg);
        Atomics.add(responsesPending, 0, 1);
        Atomics.notify(responsesPending, 0);
    }
    function sendControlMessage(controlData) {
        logger.debug(`sending control message to client ${JSON.stringify(controlData)}`);
        send({ kind: MessageKind.Control, controlData });
    }
    function sendResponse(data) {
        logger.debug(`sending response to client ${JSON.stringify(data)}`);
        send({ kind: MessageKind.Data, data });
    }
    const logging = { threshold: LogLevel.None };
    async function stopLogging() {
        if (logging._fileHandle) {
            await logging._fileHandle.close();
            logging._fileHandle = undefined;
            logging.stream = undefined;
        }
    }
    async function configureLogging({ logFile, logThreshold }) {
        logger.debug("Logging configuration has changed, stopping current logging");
        await stopLogging();
        logging.threshold = logThreshold;
        if (logging.threshold !== LogLevel.None) {
            logging._fileHandle = await fs.open(logFile, "a");
            logging.stream = logging._fileHandle.createWriteStream();
            logger.debug(`Opened file ${logFile} for logging`);
        }
    }
    function writeLogMessage(message) {
        logging.stream?.write(`${new Date().toISOString()}: ${message}\n`);
    }
    function logMessage(level, message) {
        if (level <= logging.threshold) {
            writeLogMessage(`[${logLevelNames[level]} - Executor] ${message}`);
        }
        if (level === LogLevel.Error) {
            // print errors to the console
            sendResponse({ kind: ResponseKind.ConsoleMessage, level, message });
        }
    }
    const logger = {
        error: partial(logMessage, LogLevel.Error),
        warn: partial(logMessage, LogLevel.Warning),
        info: partial(logMessage, LogLevel.Info),
        debug: partial(logMessage, LogLevel.Debug),
    };
    let socketFunc;
    try {
        if (inProcess) {
            // Assume that cloudAgent is in ccs_base
            socketFunc = getInProcessSocket(path.join(cloudAgentDir, ".."));
        }
        else {
            socketFunc = requireCloudAgentNodeModule(cloudAgentDir, "ws");
        }
    }
    catch (err) {
        sendControlMessage({ kind: ExecutorControlMessageKind.Init, error: err.message });
    }
    const modules = {};
    function connectToModule(moduleKey, port, subProtocol) {
        const ws = new socketFunc(`ws://127.0.0.1:${port}`, subProtocol);
        function sendModuleResponse(msg) {
            sendResponse({ moduleKey, ...msg, });
        }
        ws.onopen = () => {
            logger.debug(`Connection established with module ${moduleKey}`);
            sendModuleResponse({ kind: ResponseKind.Connected });
            ws.onmessage = ({ data: rawData }) => {
                const data = JSON.parse(rawData);
                logger.debug(`Received message from module ${moduleKey}: ${util.inspect(rawData)}`);
                sendModuleResponse({ kind: ResponseKind.ModuleMessage, data });
            };
        };
        ws.onclose = () => {
            logger.debug(`Connection to module ${moduleKey} was closed`);
            sendModuleResponse({ kind: ResponseKind.Closed });
            delete modules[moduleKey];
        };
        return {
            sendOnWS: (data) => {
                try {
                    const dataStr = JSON.stringify(data);
                    logger.debug(`Sending message to module ${moduleKey}: ${dataStr}`);
                    ws.send(dataStr);
                }
                catch (err) {
                    sendModuleResponse({ kind: ResponseKind.Closed });
                }
            },
            closeWS: () => {
                ws.close();
            },
        };
    }
    function handleModuleRequest(request) {
        const { kind, moduleKey } = request;
        switch (kind) {
            case RequestKind.Connect:
                modules[moduleKey] = connectToModule(moduleKey, request.port, request.subProtocol);
                logger.debug(`Connecting to module ${moduleKey} on port ${request.port} with subprotocol ${request.subProtocol || "<none>"}`);
                break;
            case RequestKind.Command:
            case RequestKind.Cancel:
                {
                    const module = modules[moduleKey];
                    if (!module) {
                        const id = "id" in request.data ? request.data.id : request.data.cancel;
                        const data = { error: id, data: { message: "Internal error, unknown module." } };
                        logger.debug("Received command request for unknown module");
                        sendResponse({ kind: ResponseKind.ModuleMessage, moduleKey, data });
                    }
                    module.sendOnWS(request.data);
                }
                break;
            case RequestKind.Close:
                {
                    const module = modules[moduleKey];
                    module.sendOnWS({ command: "close" });
                }
                break;
            default:
                assertUnreachable(kind);
        }
    }
    async function handleRequest(request) {
        const { kind } = request;
        switch (kind) {
            case RequestKind.Connect:
            case RequestKind.Command:
            case RequestKind.Cancel:
            case RequestKind.Close:
                handleModuleRequest(request);
                break;
            case RequestKind.LaunchCloudAgent:
                if (inProcess) {
                    // port number doesn't matter, return an arbitrary value
                    sendResponse({ kind: ResponseKind.CloudAgentPort, data: { port: 1 } });
                }
                else {
                    try {
                        const port = await startCloudAgent(cloudAgentDir);
                        sendResponse({ kind: ResponseKind.CloudAgentPort, data: { port } });
                    }
                    catch (err) {
                        sendResponse({ kind: ResponseKind.CloudAgentPort, data: { error: err.message } });
                    }
                }
                break;
            case RequestKind.ConfigureLogging:
                try {
                    await configureLogging(request);
                    sendResponse({ kind: ResponseKind.LoggingConfiguration });
                }
                catch (err) {
                    sendResponse({ kind: ResponseKind.LoggingConfiguration, error: err.message });
                }
                break;
            case RequestKind.Log:
                writeLogMessage(request.message);
                break;
            default:
                assertUnreachable(kind);
        }
    }
    async function handleControlMessage(msg) {
        const { kind } = msg;
        switch (kind) {
            case ControllerControlMessageKind.Shutdown:
                await stopLogging();
                sendControlMessage({ kind: ExecutorControlMessageKind.Shutdown });
                break;
            default:
                assertUnreachable(kind);
        }
    }
    async function handleMessage(msg) {
        const { kind } = msg;
        switch (kind) {
            case MessageKind.Control:
                logger.debug(`Received control message from client ${JSON.stringify(msg.controlData)}`);
                await handleControlMessage(msg.controlData);
                break;
            case MessageKind.Data:
                logger.debug(`Received request from client ${JSON.stringify(msg.data)}`);
                await handleRequest(msg.data);
                break;
            default:
                logger.debug(`Received unexpected message from client ${JSON.stringify(msg)}`);
                assertUnreachable(kind);
        }
    }
    requestReceiver.on("message", (msg) => { void handleMessage(msg); });
    function requireCloudAgentNodeModule(cloudAgentDir, module_name) {
        return require(path.join(cloudAgentDir, "src", "node_modules", module_name));
    }
    function getInProcessSocket(ccsBase) {
        // In order for dependent DLLs to be found, we need to have DebugServer/bin as the
        // working dir, or in the PATH.
        const dsBinDir = path.resolve(path.join(ccsBase, "DebugServer", "bin"));
        function fixPath() {
            if (process.env.PATH.indexOf(dsBinDir) === -1) {
                process.env.PATH += `;${dsBinDir}`;
            }
        }
        fixPath();
        // eslint-disable-next-line @typescript-eslint/no-var-requires
        const dsSocketAdapter = require(path.join(dsBinDir, "DSLiteNode")).dsSocketAdapter;
        (0, util_1.inherits)(dsSocketAdapter, events_1.EventEmitter);
        // process.env.Path doesn't stay changed for some reason, so we wrap the constructor
        class SocketAdapter extends dsSocketAdapter {
            constructor(address, protocol) {
                fixPath();
                super(address, protocol);
            }
        }
        return SocketAdapter;
    }
    function startCloudAgent(cloudAgentDir) {
        return new Promise((resolve, reject) => {
            if (!path.isAbsolute(cloudAgentDir)) {
                cloudAgentDir = path.join(process.cwd(), cloudAgentDir);
            }
            function stdoutHandler(data) {
                const dataStr = data.toString();
                logger.debug(`CloudAgent stdout: ${dataStr}`);
                if (dataStr.indexOf("Error") > -1) {
                    reject(new Error(dataStr));
                    return;
                }
                try {
                    const dataObj = JSON.parse(dataStr);
                    if (dataObj.port) {
                        logger.debug(`CloudAgent listening on port ${dataObj.port}`);
                        resolve(Number(dataObj.port));
                    }
                }
                catch (e) {
                    // ignore non json data
                }
            }
            try {
                const node = path.join(cloudAgentDir, "node");
                const script = path.join(cloudAgentDir, "src", "main.js");
                logger.debug(`spawning ${node} ${script}`);
                const cloudAgent = (0, child_process_1.spawn)(node, [script], { detached: true, env: { ...process.env, NODE_OPTIONS: "" } });
                cloudAgent.stdout.on("data", stdoutHandler);
                cloudAgent.stderr.on("data", (data) => {
                    const dataStr = data.toString();
                    logger.warn(`CloudAgent stderr: ${dataStr}`);
                });
                cloudAgent.on("error", (err) => {
                    logger.error(`Failed to launch CloudAgent: ${err.toString()}`);
                    reject(err);
                });
            }
            catch (err) {
                logger.error(err.message);
                reject(err);
            }
        });
    }
    // Indicate that we fully initialized
    sendControlMessage({ kind: ExecutorControlMessageKind.Init });
}
if (!worker_threads_1.isMainThread && isExecutorInitData(worker_threads_1.workerData)) {
    runExecutorThread(worker_threads_1.workerData);
}
